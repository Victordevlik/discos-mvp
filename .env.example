# Puerto del servidor HTTP
PORT=3000

# PIN global opcional para staff (solo para emergencia/testing)
STAFF_PIN=0101

# Clave admin para el Panel Admin (cámbiala en producción)
ADMIN_SECRET=changeme

# Origen permitido para llamadas cross-origin a la API (opcional).
# Si se deja vacío, la API solo responde a peticiones del mismo origen (recomendado).
# CORS_ALLOWED_ORIGIN=https://miapp.example.com

